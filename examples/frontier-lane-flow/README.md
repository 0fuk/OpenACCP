# Frontier Lane Flow Example

This is a concept example, not a complete strict-validation fixture.

A Frontier lane prepares and dispatches scoped docs work but cannot claim final acceptance.

Use `authority-charter.json` to see the B2 lane-local authority shape. Add concrete task cards, worker handoffs, and review reports before treating the lane as executable.
